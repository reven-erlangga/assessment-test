import { observe } from "mobx";
import { fetchTickets } from "../stores/ticket.store";

const SearchTicket = (() => {
    return (
        <div>
            {/* <button onClick={fetchTickets}>1</button> */}
        </div>
    );
});

export default SearchTicket;    